'use strict';

var squareRoot = function(a) { //x1
    let d = 6;
    let c = b;
    let b = a / 2; //x0

while (a <= 1) {
    b = (c + a / b) /2);
}
};

//var a = Number(prompt('What is the square root of'));
console.log(squareRoot(9));